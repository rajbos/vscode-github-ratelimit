{
	"version": "2.0.0",
	"tasks": [
		{
			"label": "build (pnpm)",
			"type": "process",
			"command": "pnpm",
			"args": ["-s", "run", "build"],
			"options": {
				"cwd": "${workspaceFolder}"
			},
			"problemMatcher": [],
			"group": "build"
		}
	]
}
